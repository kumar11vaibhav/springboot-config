# Server config
server.port=8089

# Database details
spring.datasource.url=jdbc:mysql://db4free.net:3306/spring11
spring.datasource.username=kumar11
spring.datasource.password=Kumar@11

# Hibernate details
spring.jpa.hibernate.ddl-auto=update

#Eureka client config
eureka.instance.client.serverUrl.defaultZone=http://localhost:8761/eureka/

# Actuator config
management.endpoints.web.exposure.include=*

# Rabbit MQ config
spring.rabbitmq.host=localhost
spring.rabbitmq.port=8777
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

key.property1=value - Department Service

# Distributed tracing config
management.tracing.sampling.probability=1.0
logging.pattern.level=%5p [${spring.application.name: },%X{traceId:-},%X{spanId:-}]
logging.level.org.springframework.debug=DEBUG
